const express = require("express");

const router = express.Router();

const status = require("../models/status");

router.post("/forword", (req, res) => {
    try {
        const Status = new status({

        })
    } catch { }
})